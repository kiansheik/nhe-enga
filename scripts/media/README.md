# Media utilities

Small project utilities that are not part of the normal build:

- `add_bookmarks.py` adds the project-specific outline to a supplied Kuapa PDF.
- `audio_compress.sh` converts a directory of WAV recordings to Opus with `ffmpeg`.

Run them from the repository root and pass source files explicitly; generated outputs should not be committed.
