# Miscellaneous research work

This directory contains historical scratchpads and experiments retained for reference. Nothing here is required by the static site, package builds, data-generation Make targets, or automated checks.

Files under `experiments/` may depend on optional packages, machine-local models, or source documents that are not distributed with the repository. They should be run from the repository root unless their header says otherwise.
